# Portal database.
# SQLite by default so local dev needs no Docker or external service.
# For PostgreSQL: change `provider` in prisma/schema.prisma and the adapter in src/client.ts.
DATABASE_URL="file:./dev.db"
